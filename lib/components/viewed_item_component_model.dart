import '/components/mobile_related_item_widget.dart';
import '/components/related_item_widget.dart';
import '/flutter_flow/flutter_flow_theme.dart';
import '/flutter_flow/flutter_flow_util.dart';
import 'package:flutter/material.dart';
import 'package:google_fonts/google_fonts.dart';
import 'package:provider/provider.dart';

class ViewedItemComponentModel extends FlutterFlowModel {
  ///  State fields for stateful widgets in this component.

  // Model for RelatedItem component.
  late RelatedItemModel relatedItemModel1;
  // Model for RelatedItem component.
  late RelatedItemModel relatedItemModel2;
  // Model for RelatedItem component.
  late RelatedItemModel relatedItemModel3;
  // Model for RelatedItem component.
  late RelatedItemModel relatedItemModel4;
  // Model for RelatedItem component.
  late RelatedItemModel relatedItemModel5;
  // Model for RelatedItem component.
  late RelatedItemModel relatedItemModel6;
  // Model for MobileRelatedItem component.
  late MobileRelatedItemModel mobileRelatedItemModel1;
  // Model for MobileRelatedItem component.
  late MobileRelatedItemModel mobileRelatedItemModel2;
  // Model for MobileRelatedItem component.
  late MobileRelatedItemModel mobileRelatedItemModel3;
  // Model for MobileRelatedItem component.
  late MobileRelatedItemModel mobileRelatedItemModel4;
  // Model for MobileRelatedItem component.
  late MobileRelatedItemModel mobileRelatedItemModel5;
  // Model for MobileRelatedItem component.
  late MobileRelatedItemModel mobileRelatedItemModel6;

  /// Initialization and disposal methods.

  void initState(BuildContext context) {
    relatedItemModel1 = createModel(context, () => RelatedItemModel());
    relatedItemModel2 = createModel(context, () => RelatedItemModel());
    relatedItemModel3 = createModel(context, () => RelatedItemModel());
    relatedItemModel4 = createModel(context, () => RelatedItemModel());
    relatedItemModel5 = createModel(context, () => RelatedItemModel());
    relatedItemModel6 = createModel(context, () => RelatedItemModel());
    mobileRelatedItemModel1 =
        createModel(context, () => MobileRelatedItemModel());
    mobileRelatedItemModel2 =
        createModel(context, () => MobileRelatedItemModel());
    mobileRelatedItemModel3 =
        createModel(context, () => MobileRelatedItemModel());
    mobileRelatedItemModel4 =
        createModel(context, () => MobileRelatedItemModel());
    mobileRelatedItemModel5 =
        createModel(context, () => MobileRelatedItemModel());
    mobileRelatedItemModel6 =
        createModel(context, () => MobileRelatedItemModel());
  }

  void dispose() {
    relatedItemModel1.dispose();
    relatedItemModel2.dispose();
    relatedItemModel3.dispose();
    relatedItemModel4.dispose();
    relatedItemModel5.dispose();
    relatedItemModel6.dispose();
    mobileRelatedItemModel1.dispose();
    mobileRelatedItemModel2.dispose();
    mobileRelatedItemModel3.dispose();
    mobileRelatedItemModel4.dispose();
    mobileRelatedItemModel5.dispose();
    mobileRelatedItemModel6.dispose();
  }

  /// Action blocks are added here.

  /// Additional helper methods are added here.
}
